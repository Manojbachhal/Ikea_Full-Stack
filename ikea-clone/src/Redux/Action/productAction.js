const thunkActionProducts = (dispatch, getState, payload) => {
  //   dispatch = {
  //     type,
  //     payload,
  //   };
  return (dispatch = {
    type: "BEDDING",
    payload,
  });
};

export default thunkActionProducts;
